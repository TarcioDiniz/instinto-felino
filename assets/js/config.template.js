/* Gerado no start do container a partir das variáveis de ambiente (envsubst).
   NÃO edite este arquivo: edite o .env (ou as envs do container). */
window.IF_CONFIG = {
  CHECKOUT_URL_1: "${CHECKOUT_URL_1}",
  CHECKOUT_URL_2: "${CHECKOUT_URL_2}",
  CHECKOUT_URL_3: "${CHECKOUT_URL_3}",
  WHATSAPP_URL:   "${WHATSAPP_URL}"
};
