/* Config padrão (uso local, sem Docker). No container, este arquivo é
   regenerado a partir do config.template.js + variáveis de ambiente.
   Para testar links localmente, basta preencher as URLs abaixo. */
window.IF_CONFIG = {
  CHECKOUT_URL_1: "",
  CHECKOUT_URL_2: "",
  CHECKOUT_URL_3: "",
  WHATSAPP_URL:   ""
};
