services:
  landing:
    build: .
    image: instinto-felino:v2
    container_name: instinto-felino
    ports:
      - "8080:80"
    env_file:
      - path: .env
        required: false
    restart: unless-stopped
